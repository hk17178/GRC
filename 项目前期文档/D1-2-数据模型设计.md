# GRC 平台 · 数据模型设计文档

**文档编号：** D1-2 ｜ **版本：** v1.0（草案） ｜ **日期：** 2026-06-22
**依据：** 需求 V1.8、D1-1 总体架构设计
**技术基线：** PostgreSQL 16（+ pgvector + JSONB）｜ 模块化单体
**状态：** 待评审

---

## 一、引言

### 1.1 目的
定义平台的逻辑/物理数据模型，**首要确立组织维度数据隔离的落库方式**（需求 7.3：隔离边界须在数据模型阶段定死，后期补救成本极高），并给出表单引擎、留痕库等关键子系统的数据结构。

### 1.2 范围
覆盖 M1–M10 的核心数据实体、组织隔离机制、版本化、留痕防篡改、分类分级。Flowable 工作流自有表由引擎托管，本文只描述平台侧与之关联的数据。

### 1.3 建模五原则
1. **组织隔离第一**：每个业务实体携带组织归属，隔离在数据访问层统一拦截 + PG 行级安全（RLS）兜底，**绝不在各模块各写一遍**。
2. **配置即数据**：模板、控件、体系、通知规则、权限、法规源均为数据（支持运营自助、零发版）。
3. **弹性与可统计兼顾**：表单答案等异构结构用 JSONB；但打分/等级等需聚合的字段**抽取为规范化列**，保证 M9 看板可聚合。
4. **版本化与快照**：制度、模板、评估、法规均版本化；评估启动即固化模板版本快照。
5. **留痕不可篡改**：证据/操作日志写入即只读、哈希校验、独立存储，保留期取各监管口径最大值。

---

## 二、逻辑数据架构（领域分区）

```
┌─ 身份与组织 ─┐  ┌─ 资产台账(M6) ─┐  ┌─ 制度(M1) ─┐  ┌─ 法规(M4) ─┐
│ org 组织树   │  │ asset 评估对象  │  │ policy     │  │ regulation │
│ app_user    │  │ (系统/流程/数据)│  │ +version   │  │ +version   │
│ (AD同步)    │  └────────────────┘  │ attestation│  │ -policy映射 │
└─────────────┘                      └────────────┘  └────────────┘
┌─ 权限(M8) ──┐  ┌──────── 风险评估与表单引擎(M2) ────────┐  ┌─ 审计(M3) ─┐
│ role        │  │ framework 体系                         │  │ audit_plan │
│ permission  │  │ control_library 控件库 + 跨体系映射     │  │ finding    │
│ user_role_  │  │ template + template_version           │  │ (检查表复用 │
│   org 绑定  │  │ section/item 结构 · scoring/report 映射 │  │  模板引擎) │
└─────────────┘  │ assessment + answer + result          │  └────────────┘
                 │ control_test_result (一次测试多体系复用) │
                 │ kri_indicator/threshold/alert (持续监控) │
                 └───────────────┬───────────────────────┘
┌─ 整改(M2/M3共用) ─┐            │            ┌─ 供应商(M7) ─┐
│ remediation_task  │◄───────────┘            │ vendor       │
│ (工单状态机)       │  发现/风险点→整改         │ +lifecycle   │
└───────────────────┘                         │ +sla +event  │
┌─ 通知(M10) ─┐  ┌─ 留痕/看板(M9) ─┐           └──────────────┘
│ notif_rule  │  │ evidence 证据    │
│ (配置)       │  │ operation_log    │  ┌─ 事件总线 ─┐
│ notif_log   │  │ (独立append-only)│  │ domain_event│ (解耦各模块→M10/M9)
│ (留痕)       │  │ 看板=物化视图     │  └────────────┘
└─────────────┘  └─────────────────┘
```

---

## 三、组织维度与数据隔离设计 ⭐地基

### 3.1 组织树（org）
统一承载集团/子公司/部门多级结构，作为隔离与通知路由的判断基础（需求 8.2）。

```sql
CREATE TABLE org (
  id          BIGINT PRIMARY KEY,            -- 雪花ID
  parent_id   BIGINT REFERENCES org(id),
  org_type    VARCHAR(16) NOT NULL,          -- GROUP集团/SUBSIDIARY子公司/DEPT部门
  code        VARCHAR(64) UNIQUE NOT NULL,
  name        VARCHAR(128) NOT NULL,
  path        VARCHAR(512) NOT NULL,         -- 物化路径 /1/12/120 便于子树查询
  ad_dn       VARCHAR(256),                  -- 对应AD的可分辨名
  status      VARCHAR(16) DEFAULT 'ACTIVE',
  ...审计字段
);
CREATE INDEX idx_org_path ON org (path text_pattern_ops);
```

### 3.2 隔离落库约定（关键）
- **每个业务实体表都带 `org_id`（所属子公司）** 与必要时 `dept_id`，作为隔离锚点。
- 隔离判断由**统一数据访问层**根据当前用户的 `角色×组织` 自动追加过滤条件（`org_id ∈ 用户可见组织集合`），**业务代码不手写隔离条件**。
- **纵深防御**：对敏感表启用 PostgreSQL **行级安全（RLS）**，即使应用层漏判，数据库层也拦截越权。
  ```sql
  ALTER TABLE assessment ENABLE ROW LEVEL SECURITY;
  CREATE POLICY p_org ON assessment USING (org_id = ANY (current_setting('app.visible_orgs')::bigint[]));
  ```
- **集团管理层 / 只读审阅角色**例外：可见组织集合=全集团（需求 10.2/10.3）。
- 可见组织集合按"用户-角色-组织"绑定 + 角色是否跨子公司汇总（集团层）计算，缓存于会话。

### 3.3 四元组权限锚点
任一数据访问 = `用户(app_user) × 角色(role) × 组织(org) × 数据(行)`；前三者决定"可见组织集合"，第四由 `org_id` 落地（详见 D1-3）。

---

## 四、身份与组织（AD 同步）

```sql
CREATE TABLE app_user (
  id          BIGINT PRIMARY KEY,
  username    VARCHAR(64) UNIQUE NOT NULL,   -- 来自AD
  display_name VARCHAR(128),
  email       VARCHAR(128),
  org_id      BIGINT REFERENCES org(id),     -- 主组织（子公司/部门）
  ad_object_id VARCHAR(64),                  -- AD唯一标识，用于增量同步
  status      VARCHAR(16) DEFAULT 'ACTIVE',  -- ACTIVE/DISABLED（离职即停用→权限回收）
  last_sync_at TIMESTAMPTZ,
  ...审计字段
);
```
- 账号与组织由 AD 单向同步（需求 8.2、10.4）；平台不独立维护账号体系。
- 离职/调岗：同步时置 `DISABLED` 或更新 `org_id`，触发权限即时回收（D1-3 详述时效）。

---

## 五、资产/对象台账（M6）

```sql
CREATE TABLE asset (
  id          BIGINT PRIMARY KEY,
  org_id      BIGINT NOT NULL REFERENCES org(id),
  asset_type  VARCHAR(24) NOT NULL,          -- SYSTEM信息系统/PROCESS业务流程/DATA数据资产/VENDOR供应商
  code        VARCHAR(64),
  name        VARCHAR(128) NOT NULL,
  owner_user_id BIGINT REFERENCES app_user(id),
  importance  VARCHAR(16),                   -- 重要性等级
  attributes  JSONB,                         -- 类型相关弹性属性
  status      VARCHAR(16) DEFAULT 'ACTIVE',
  ...审计字段
);
```
- 作为 M2/M3 评估对象的**唯一来源**（需求 8.2），评估记录可追溯至具体资产。
- 供应商对象（VENDOR）纳入同一台账（需求 9.2），细节扩展见 §十一。

---

## 六、制度体系（M1）

```sql
-- 制度主体
CREATE TABLE policy (
  id          BIGINT PRIMARY KEY,
  org_id      BIGINT NOT NULL REFERENCES org(id),
  code        VARCHAR(64),
  name        VARCHAR(256) NOT NULL,
  framework   VARCHAR(32)[],                 -- 所属体系（ISO27001/等保/PIPL/PBOC…）多值
  level       VARCHAR(16),                   -- 集团级/子公司级/部门级
  owner_dept  BIGINT REFERENCES org(id),
  is_core     BOOLEAN DEFAULT false,         -- 是否核心制度（决定强制签署范围）
  review_cycle_months INT,                   -- 复审周期
  current_version_id BIGINT,                 -- 指向当前生效版本
  status      VARCHAR(16),                   -- DRAFT/REVIEW/EFFECTIVE/DEPRECATED
  ...审计字段
);
-- 版本（保留完整历史）
CREATE TABLE policy_version (
  id          BIGINT PRIMARY KEY,
  policy_id   BIGINT NOT NULL REFERENCES policy(id),
  version_no  VARCHAR(16) NOT NULL,
  effective_date DATE,
  content_ref VARCHAR(256),                  -- 正文存储引用（MinIO）
  change_type VARCHAR(16),                   -- MAJOR(触发重新确认)/MINOR(仅元数据)
  status      VARCHAR(16),
  ...审计字段
);
-- 引用关系（下位文件引用上位条款）
CREATE TABLE policy_reference (
  from_policy_id BIGINT, from_clause VARCHAR(64),
  to_policy_id   BIGINT, to_clause   VARCHAR(64)
);
-- 签署确认记录（合规证据，写入留痕库）
CREATE TABLE policy_attestation (
  id          BIGINT PRIMARY KEY,
  policy_version_id BIGINT NOT NULL REFERENCES policy_version(id),
  user_id     BIGINT NOT NULL REFERENCES app_user(id),
  org_id      BIGINT NOT NULL,
  confirmed_at TIMESTAMPTZ,                  -- 确认时间
  version_no_snapshot VARCHAR(16),           -- 确认时的版本号
  status      VARCHAR(16)                    -- PENDING/CONFIRMED
);
```
- 强制确认范围按 `is_core` + 制度类型配置（需求 3.2.2，限定核心制度，避免确认疲劳）。
- `change_type=MAJOR` 触发重新确认流程。
- 知识库切片（向量）见 §十四。

---

## 七、风险评估与表单引擎（M2）⭐最高难点

> 表单引擎是全项目核心难点。数据模型采用"**结构规范化 + 答案 JSONB + 打分抽取列**"混合策略：结构与控件规范化以支持引用/统计/复用，答案弹性存 JSONB，打分结果抽取为列以支撑看板聚合。

### 7.1 体系与控件库

```sql
-- 体系（内置脚手架 + 自定义新增）
CREATE TABLE framework (
  id BIGINT PRIMARY KEY, code VARCHAR(32) UNIQUE, name VARCHAR(128),
  is_builtin BOOLEAN, ...审计字段                -- 等保/ISO27001/PBOC/PCI/27701/27018/9001/供应商 + 自定义
);
-- 统一控件库（"具体控制点"颗粒度，需求4.2.2/V1.7）
CREATE TABLE control_library (
  id BIGINT PRIMARY KEY,
  code VARCHAR(64) UNIQUE,                       -- CTL-007
  name VARCHAR(256),                             -- “特权账号定期审查”
  category VARCHAR(64),                          -- 控制域分类（仅归类用，不替代控制点颗粒度）
  description TEXT, ...审计字段
);
-- 控件↔体系跨映射（一个控件对应多体系的不同条款/编号）
CREATE TABLE control_framework_map (
  control_id BIGINT REFERENCES control_library(id),
  framework_id BIGINT REFERENCES framework(id),
  clause_code VARCHAR(64),                       -- 该体系下的条款/控制点编号
  PRIMARY KEY (control_id, framework_id)
);
```

### 7.2 模板（结构配置层 + 打分/报告映射层，两层解耦）

```sql
CREATE TABLE template (
  id BIGINT PRIMARY KEY,
  framework_id BIGINT REFERENCES framework(id),
  usage VARCHAR(16),                             -- ASSESSMENT风险评估 / AUDIT审计（同一引擎按用途区分，需求5.2.2）
  name VARCHAR(128),
  current_version_id BIGINT, ...审计字段
);
CREATE TABLE template_version (
  id BIGINT PRIMARY KEY,
  template_id BIGINT REFERENCES template(id),
  version_no VARCHAR(16),
  effective_date DATE,
  status VARCHAR(16),                            -- DRAFT/EFFECTIVE/RETIRED
  report_format JSONB,                           -- 报告章节顺序/措辞规范（随版本固化）
  scoring_model JSONB,                           -- 打分模型（定量:可能性×影响 / 定性等级 / 是否适用）
  ...审计字段
);
-- 结构配置层：层级（域→控制项），自引用
CREATE TABLE template_section (
  id BIGINT PRIMARY KEY,
  version_id BIGINT REFERENCES template_version(id),
  parent_id BIGINT REFERENCES template_section(id),
  seq INT, name VARCHAR(256),
  level_label VARCHAR(32)                         -- 命名可配置（如“控制点编号”）
);
-- 结构配置层：题目；可引用控件库条目（复用），也可自定义
CREATE TABLE template_item (
  id BIGINT PRIMARY KEY,
  section_id BIGINT REFERENCES template_section(id),
  control_id BIGINT REFERENCES control_library(id),  -- 引用控件库（NULL=自定义题）
  seq INT,
  question TEXT,
  item_type VARCHAR(24),                          -- SINGLE/MULTI/SCORE/TEXT/MATRIX/ATTACH
  options JSONB,                                  -- 选项/矩阵定义
  required BOOLEAN,
  skip_logic JSONB,                              -- 跳题逻辑（答“不适用”跳过关联题）
  -- 打分/报告映射层（与结构解耦，但随题维护）：
  weight NUMERIC(6,3),                           -- 权重
  report_section_key VARCHAR(64),                -- 得分归入报告的哪个章节（需求4.2.2）
  severity_suggest VARCHAR(16)                   -- 判不合规时建议严重度（审计场景，仅参考）
);
```
- **两层解耦**：结构（section/item 的层级、题型、跳题）与 打分/报告映射（weight、report_section_key、scoring_model、report_format）分列存储，调整报告格式不牵连表单字段，反之亦然（需求 4.2.2/4.4）。
- **发布前测试**：`template_version.status=DRAFT` 时可预览/测试填写，验证通过才置 `EFFECTIVE`（需求 4.2.2 配置治理）。

### 7.3 评估实例与答案（启动即快照版本）

```sql
CREATE TABLE assessment (
  id BIGINT PRIMARY KEY,
  org_id BIGINT NOT NULL REFERENCES org(id),
  asset_id BIGINT REFERENCES asset(id),          -- 评估对象（来自台账）
  template_version_id BIGINT REFERENCES template_version(id), -- 固化的版本快照
  plan_type VARCHAR(16),                          -- PERIODIC周期/AD_HOC专项
  owner_user_id BIGINT,
  due_date DATE,
  status VARCHAR(16),                             -- FILLING/REVIEW/APPROVED/EFFECTIVE
  -- 抽取的可聚合结果列（供M9看板）：
  risk_score NUMERIC(8,2),
  risk_level VARCHAR(10),                         -- 五级 VERY_LOW/LOW/MID/HIGH/VERY_HIGH（极低/低/中/高/极高，见 CR-001-7）
  approved_by BIGINT, approved_at TIMESTAMPTZ,
  ...审计字段
);
CREATE TABLE assessment_answer (
  id BIGINT PRIMARY KEY,
  assessment_id BIGINT REFERENCES assessment(id),
  item_id BIGINT,                                -- 对应快照版本的题目
  value JSONB,                                   -- 弹性答案
  score NUMERIC(8,2),                            -- 抽取得分
  attachment_keys VARCHAR(256)[],                -- 佐证附件（MinIO）
  control_id BIGINT                              -- 冗余控件引用，支撑“一次测试多体系复用”
);
-- 控件测试结果（跨体系复用：同一控件被多体系评估，一次结果多处引用，需求4.2.2）
CREATE TABLE control_test_result (
  id BIGINT PRIMARY KEY,
  org_id BIGINT NOT NULL,
  asset_id BIGINT, control_id BIGINT,
  result VARCHAR(16),                            -- 符合/部分不符/不符合
  tested_at TIMESTAMPTZ, evidence_id BIGINT,
  reuse_window_days INT                          -- 复用有效期
);
```

### 7.4 报告生成
报告由 `scoring_model + report_format + report_section_key + answers` 驱动动态生成（需求 4.2.3），**不为每个体系单独开发报告逻辑**；人工复核签批后 `status=EFFECTIVE`；导出 Word/PDF 异步生成。

### 7.5 KRI 持续监控（M2）

```sql
CREATE TABLE kri_indicator (
  id BIGINT PRIMARY KEY, org_id BIGINT, name VARCHAR(128),
  source VARCHAR(24),                             -- SIEM/LOG/VULN（采集来源，需求4.2.5）
  metric_key VARCHAR(64), threshold JSONB, ...审计字段
);
CREATE TABLE kri_alert (
  id BIGINT PRIMARY KEY, indicator_id BIGINT,
  reading NUMERIC, breached BOOLEAN, occurred_at TIMESTAMPTZ,
  evidence_id BIGINT                             -- 预警记录入留痕
);
```

---

## 八、整改任务（M2/M3 共用工单）

> 风险点与审计发现**共用同一整改机制**（需求 4.2.4/5.2.1），避免两套流程。

```sql
CREATE TABLE remediation_task (
  id BIGINT PRIMARY KEY,
  org_id BIGINT NOT NULL,
  source_type VARCHAR(16),                        -- ASSESSMENT/AUDIT/KRI
  source_id BIGINT,                              -- 风险点/发现项/预警
  title VARCHAR(256),
  assignee_id BIGINT, due_date DATE, priority VARCHAR(8),
  state VARCHAR(16),                             -- 状态机：TODO/DOING/DONE/OVERDUE/VERIFIED
  verify_by BIGINT, verified_at TIMESTAMPTZ,
  proof_evidence_id BIGINT,                      -- 整改证明（验证后才能关闭，入留痕）
  ...审计字段
);
```
状态机：`TODO→DOING→DONE→(发起人验证)→VERIFIED`；超期自动 `OVERDUE` 并经 M10 通知。

---

## 九、内部审计（M3）

```sql
CREATE TABLE audit_plan (
  id BIGINT PRIMARY KEY, org_id BIGINT, theme VARCHAR(128),
  asset_id BIGINT, template_version_id BIGINT,   -- 检查表=复用模板引擎(usage=AUDIT)
  status VARCHAR(16), ...审计字段
);
CREATE TABLE audit_finding (
  id BIGINT PRIMARY KEY, audit_plan_id BIGINT,
  description TEXT,
  clause_ref VARCHAR(64),                        -- 依据条款（可引用M1制度条款，需求5.2.2）
  policy_clause_id BIGINT,                       -- 关联制度条款，报告自动带出编号
  severity VARCHAR(16),
  evidence_id BIGINT,                            -- 证据材料
  -- 整改复用 remediation_task（source_type=AUDIT）
  ...审计字段
);
```
- 检查表执行=填写 assessment（usage=AUDIT）；报告复用 §7.4 机制。
- 历史审计趋势分析（同类问题反复）= 基于 `clause_ref/category` 的聚合查询。

---

## 十、法律法规（M4）

```sql
CREATE TABLE regulation_source (
  id BIGINT PRIMARY KEY, name VARCHAR(128),       -- 中国人民银行/网信办/金融监管总局…
  url VARCHAR(512), crawl_rule JSONB,            -- 采集规则（配置化，网站改版改配置）
  health_status VARCHAR(16), last_ok_at TIMESTAMPTZ
);
CREATE TABLE regulation (
  id BIGINT PRIMARY KEY, source_id BIGINT, title VARCHAR(512),
  current_version_id BIGINT, ...审计字段
);
CREATE TABLE regulation_version (
  id BIGINT PRIMARY KEY, regulation_id BIGINT, version_no VARCHAR(16),
  published_at DATE, change_summary TEXT,        -- 条款级变更摘要（可由M5提取，需求6.4）
  content_ref VARCHAR(256)
);
-- 法规条款 ↔ 内部制度条款 映射（逐条合规核查，需求6.2）
CREATE TABLE regulation_policy_map (
  regulation_id BIGINT, reg_clause VARCHAR(64),
  policy_id BIGINT, policy_clause VARCHAR(64),
  PRIMARY KEY (regulation_id, reg_clause, policy_id, policy_clause)
);
```

---

## 十一、第三方/供应商（M7）

```sql
CREATE TABLE vendor (
  id BIGINT PRIMARY KEY, org_id BIGINT,
  asset_id BIGINT,                               -- 纳入M6台账（asset_type=VENDOR）
  name VARCHAR(128), category VARCHAR(64),
  lifecycle VARCHAR(16),                         -- ONBOARDING/IN_CONTRACT/RENEWAL/EXIT
  risk_score NUMERIC, ...审计字段
);
CREATE TABLE vendor_sla (id BIGINT PRIMARY KEY, vendor_id BIGINT, metric VARCHAR(64), target JSONB, expire_date DATE);
CREATE TABLE vendor_event (                       -- 事件触发非计划复评（需求9.2）
  id BIGINT PRIMARY KEY, vendor_id BIGINT,
  event_type VARCHAR(32),                        -- 数据泄露/信用下调/监管处罚
  source VARCHAR(16),                            -- MANUAL人工登记/EXTERNAL外部接口
  triggered_reassessment BIGINT                  -- 触发的复评(assessment)
);
```
- 供应商评估复用 M2 表单引擎（供应商专用模板，定量叠加定性）。

---

## 十二、统一通知中心（M10）

```sql
-- 规则（配置化，事件→级别→通道，需求12.2.2）
CREATE TABLE notification_rule (
  id BIGINT PRIMARY KEY, source_module VARCHAR(8),    -- M2/M3/M4
  event_type VARCHAR(64), level VARCHAR(8),           -- 紧急/重要/普通
  channels VARCHAR(16)[],                             -- WECOM/FEISHU/EMAIL/SMS
  org_scope JSONB,                                    -- 与组织/权限联动，防跨子公司广播
  enabled BOOLEAN, dedup_window_sec INT,             -- 降噪合并窗口
  ...审计字段
);
-- 发送记录（完整留痕，写入M9，监管举证“是否及时通知”）
CREATE TABLE notification_log (
  id BIGINT PRIMARY KEY, rule_id BIGINT, event_id BIGINT,
  target JSONB,                                       -- 送达对象
  channel VARCHAR(16), content TEXT,
  sent_at TIMESTAMPTZ, result VARCHAR(16),            -- SUCCESS/FAIL/RETRIED
  retry_count INT, evidence_id BIGINT
);
```
- 通道为适配器，单向推送（需求 12.2.1）；短信受级别控制（成本）。

---

## 十三、证据留痕库 & 看板（M9）⭐防篡改

```sql
-- 证据（写入即只读 + 哈希 + 保留期取最大值）
CREATE TABLE evidence (
  id BIGINT PRIMARY KEY,
  ref_type VARCHAR(24),                          -- ASSESSMENT/AUDIT/REMEDIATION/ATTESTATION/NOTIFICATION/KRI
  ref_id BIGINT,
  org_id BIGINT,
  file_key VARCHAR(256),                         -- MinIO 对象键
  sha256 CHAR(64) NOT NULL,                      -- 入库即生成校验值（需求11.3）
  size_bytes BIGINT,
  created_by BIGINT, created_at TIMESTAMPTZ DEFAULT now(),
  retention_until DATE NOT NULL,                 -- 保留期=各监管口径最大值（需求11.2.2）
  immutable BOOLEAN DEFAULT true                 -- 标记只读
);
-- 操作日志：独立、仅追加、不可删改（应用账号无 DELETE/UPDATE 权限）
CREATE TABLE operation_log (
  id BIGSERIAL PRIMARY KEY,
  actor_id BIGINT, action VARCHAR(64), target_type VARCHAR(32), target_id BIGINT,
  org_id BIGINT, detail JSONB, occurred_at TIMESTAMPTZ DEFAULT now()
);
-- 通过数据库权限/触发器禁止对 evidence、operation_log 的 UPDATE/DELETE
REVOKE UPDATE, DELETE ON evidence, operation_log FROM app_rw;
```
- **不可篡改不仅靠应用层**：数据库层回收应用账号的 UPDATE/DELETE 权限 + 触发器拦截（需求 11.4：不能仅依赖应用逻辑）。
- 看板（M9 11.2.1）多为**物化视图/聚合查询**，按组织/时间下钻，标注数据截止时间；不一定独立建表。

---

## 十四、知识库切片与向量（M1 / M5，pgvector）

```sql
CREATE TABLE kb_chunk (
  id BIGINT PRIMARY KEY,
  source_type VARCHAR(16),                       -- POLICY/REGULATION
  source_version_id BIGINT,                      -- 关联制度版本/法规版本
  clause_no VARCHAR(64),                         -- 保留条款编号（避免断章取义，需求3.4）
  text TEXT,
  embedding vector(1024),                        -- pgvector 向量
  org_id BIGINT,                                 -- 用于问答时组织级过滤
  is_current BOOLEAN                             -- 默认仅检索当前生效版本
);
CREATE INDEX ON kb_chunk USING hnsw (embedding vector_cosine_ops);
```
- 切片按**章节/条款边界**（需求 3.4，核心难点），保留条款编号与上下文。
- M5 问答检索时强制叠加 `org_id ∈ 可见组织` 过滤（安全红线，D1-3）。

---

## 十五、权限模型（M8，详见 D1-3）

```sql
CREATE TABLE role (id BIGINT PRIMARY KEY, code VARCHAR(64), name VARCHAR(128), is_group_level BOOLEAN); -- 20个角色（需求10.2）
CREATE TABLE permission (id BIGINT PRIMARY KEY, module VARCHAR(8), action VARCHAR(16)); -- C/R/U/A
CREATE TABLE role_permission (role_id BIGINT, permission_id BIGINT);
-- 用户-角色-组织 三元绑定（同一角色在不同子公司数据相互隔离，需求10.4）
CREATE TABLE user_role_org (
  user_id BIGINT, role_id BIGINT, org_id BIGINT,
  PRIMARY KEY (user_id, role_id, org_id)
);
```
- 审批流定义由 Flowable 托管；平台侧仅以 `process_key + business_key` 关联评估报告/制度发布/整改签批等单据。

---

## 十六、通用设计约定

| 约定 | 规则 |
|---|---|
| 主键 | 雪花 ID（BIGINT）；高频追加日志可用 BIGSERIAL |
| 审计字段 | 每表含 `created_by/created_at/updated_by/updated_at` |
| 软删除 | 业务表 `status` 标记，不物理删除（留痕表禁删） |
| 并发 | 关键单据乐观锁 `version` 列 |
| 组织字段 | 业务表统一 `org_id`（隔离锚点），子树查询用 org.path |
| JSONB 用法 | 仅用于真异构/弹性结构（答案、规则、属性）；可聚合字段必须抽列 |
| 命名 | 表/列 snake_case；枚举值大写常量 |
| 时间 | 一律 `TIMESTAMPTZ`（UTC 存储） |
| 索引 | org_id、状态、时间、外键、看板聚合维度建索引；向量用 HNSW |

---

## 十七、数据分类分级与脱敏（需求 §14.1）
- 分级：公开 / 内部 / 敏感；子公司风险评估数据、审计发现、证据材料 = 敏感。
- 敏感数据访问受 M8 约束并记 `operation_log`；跨子公司汇总统计场景对明细脱敏。
- 字段级标注（可在元数据登记敏感字段），导出/问答时按级别控制。

---

## 十八、版本化、保留与归档
- 版本化：制度、模板、评估快照、法规、知识库切片。
- 保留期：留痕 `retention_until` = 各适用监管口径中**最长**（需求 11.2.2），具体数值在监管条款核实后固化为平台参数。
- 到期处理：到期数据**人工确认后归档**而非自动清除（需求 11.4 关键风险：避免监管延期检查时无法提供）。
- 历史迁移：Excel/Word 历史记录导入，明确字段映射；无法结构化的至少作附件留存（需求 14.3，详见 D2-4）。

---

## 十九、ER 核心关系（文字摘要）

- `org 1—* app_user`、`org 1—* asset`、`org` 自引用成树。
- `policy 1—* policy_version 1—* policy_attestation`；`policy_version *—* policy`（引用关系）。
- `framework 1—* template 1—* template_version 1—* template_section 1—* template_item`。
- `control_library *—* framework`（control_framework_map）；`template_item *—1 control_library`。
- `template_version 1—* assessment 1—* assessment_answer`；`assessment *—1 asset`。
- `assessment/audit_finding/kri_alert 1—* remediation_task`（source 多态）。
- `regulation 1—* regulation_version`；`regulation *—* policy`（regulation_policy_map）。
- `vendor 1—1 asset`、`vendor 1—* vendor_event/sla`。
- `notification_rule 1—* notification_log`；`* → evidence`（多态 ref）。
- `role/permission/user_role_org` 构成 RBAC；`kb_chunk → policy_version/regulation_version`。

---

## 二十、评审重点
1. **组织隔离落库方案**：统一拦截 + RLS 兜底是否认可；可见组织集合的计算与缓存策略。
2. **表单引擎模型**：结构规范化 + 答案 JSONB + 打分抽列 的混合策略是否满足"通用配置 + 可聚合统计 + 控件复用"。
3. **留痕不可篡改**：数据库层回收 UPDATE/DELETE + 触发器，是否满足监管举证强度。
4. **保留期取最大值** 的具体监管口径清单（需逐条核实后固化）。
5. JSONB 与规范化边界（哪些必须抽列）。

> 下一份：**D1-3 安全设计**（认证/RBAC 四元组/数据隔离与 AI 越权防护/加密/审计/凭证安全），与本文 §3/§13/§15 紧密衔接。

---

## 二十一、补充实体（CR-001 / CR-002 增补）

> 以下为评审与需求增补新增/扩展的实体，均含 `org_id` 组织隔离锚点，沿用 §三 隔离与 §十六 通用约定。

**监管事项（新增模块 M-REG）**
- `reg_filing` 监管报送：类型(月报/季报/年报/临时)、监管机构、法定时限、责任人、状态；
- `reg_inquiry` 监管问询：来源、事项、收文日、答复期限、答复留痕(evidence)、状态；
- `reg_penalty` 处罚/约谈：类型、监管机构、事由、整改要求、回函状态；
- `major_incident_report` 重大事件报送：事件、报送时限、版本、回执留痕。

**合规义务清单**：`obligation`（来源法规条款 → 义务 → 责任部门 → 关联制度/控件 → 证据 → 满足状态）——端到端举证链头部。

**个人信息处理活动（ROPA，PIPL 法定台账）**：`pi_processing_activity`（活动、目的、PI 类型、数据量级、是否敏感、是否出境、保留期、责任人）。

**外部审计**：`audit` 增 `audit_type`(内审/外审/监管检查/认证)；新增 `external_finding`；外审整改增 `external_status`(已提交外部机构/外方受理/外方确认关闭)。

**供应商评估闭环**：复用 `assessment`，新增 `assessment.kind`(准入/年度定期/续约/退出/事件复评)，target=vendor；复用整改工单闭环。

**资产合规属性**：`asset` 增 `data_class`(公开/内部/敏感)、`has_pi`、`cross_border`、`mlps_level`(等保定级)、`mlps_record`(定级备案)、`chd`(持卡人数据)。

**风险处置与残余**：`assessment` 增 `inherent_level`、`treatment_decision`(降低/接受/转移/规避)、`residual_level`、`risk_acceptance`(管理层接受签认)。

**A-T-V 建模**：`asset_threat_vuln`（资产-威胁源-脆弱性-可能性-影响，关联评估）。

**权限审阅(UAR)**：`access_review`（审阅活动：范围/周期/复核人/保留或回收/签认证据）；越权明细沿用 `operation_log`。

**自定义看板**：`dashboard_widget_config`（用户/角色 → 组件与信息流布局，支持自定义上屏）。

**风险五级**：`risk_level` 枚举更新为 `VERY_LOW/LOW/MID/HIGH/VERY_HIGH`（极低/低/中/高/极高，见 §七 与 CR-001-7）。

**国际化(i18n)**：界面文案外置语言包（非数据库）；制度/法规等内容级可选双语字段按需。
