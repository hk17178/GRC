-- =============================================================
-- V83 · 清理陈旧配置键 risk.matrix.size（用户反馈 ①：可改项应真实有效）
-- =============================================================
-- 背景：风险矩阵五级档位已上收为 risk.matrix.bands（V62，系统锁定·唯一事实源），
--   代码中仅 RiskMatrix/RiskMatrixBootstrap 读取 bands，不再读取 size。
--   早期集成库残留一条可改的 risk.matrix.size=6，改它无任何效果——属误导性"死配置"。
--   评估报告 C3 已建议"锁定或删除 risk.matrix.size 键"。此处删除。
-- 幂等：全新库本无该行（删除 0 行），集成库删除残留行。
-- =============================================================

DELETE FROM system_setting WHERE setting_key = 'risk.matrix.size';
